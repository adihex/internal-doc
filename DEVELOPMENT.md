# Development note

TDD baseline: tests were authored before implementation. `npm test` initially failed because Vitest and implementation dependencies were not installed/present. Implementation followed that recorded red baseline.
